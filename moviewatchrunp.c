#include <stdio.h>

int main() {
    int members = 3;
    int rate = 500;
    int totalBill = members * rate;

    printf("Total bill for %d members: %d\n", members, totalBill);

    return 0;
}
